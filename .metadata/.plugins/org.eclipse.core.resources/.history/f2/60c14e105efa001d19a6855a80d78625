#include <stdio.h>

void printCrossroads(int count) {
    if (count < 8) {
        printf("Crossroads\n");
        printCrossroads(count + 1);
    }
}

int main() {
	char s[100];
	gets(s);
    printCrossroads(s, 0);
    return 0;
}
