#include <stdio.h>

void printCrossroads(int count) {
	char s[100];
	setbuf(stdout, NULL);
	printf("Enter the word: \n");
	scanf("%s", &s);
    if (count < 8) {
        printf("%s \n", s);
        printCrossroads(count + 1);
    }
}

int main() {
    printCrossroads(1);
    return 0;
}
