#include <stdio.h>

int main() {

	int i, j, n;
	setbuf(stdout, NULL);

	printf("Enter rows: ");
	scanf("%d", &n);

	for (i = 0; i < n; i++) {

		for (j = 0; j < n; j++) {

			if ( i == j) || (i == j+i+1)
				printf("* ");
			else
				printf("  ");
		}

		printf("\n");

	}
		return 0;

}
